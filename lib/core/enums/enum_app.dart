
enum AppLanguage { tr, en }
enum ThemeApperance { light, dark }
enum ResponseType { error, success }
enum ApiMethod { get, post, put, delete, multipart }
enum ActivityState { isLoading, isLoaded, isError }
enum AlertTypes { toast, warning, error, success}
